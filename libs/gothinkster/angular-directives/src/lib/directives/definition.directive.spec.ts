import { DefinitionDirective } from './definition.directive';

describe('DefinitionDirective', () => {
  it('should create an instance', () => {
    const directive = new DefinitionDirective();
    expect(directive).toBeTruthy();
  });
});
